<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:OpenMotics.App.Controls;assembly=OpenMotics.App"
             x:Class="OpenMotics.App.Views.DevicesView">
  <ContentPage.Content>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition></RowDefinition>
        <RowDefinition Height="100"></RowDefinition>
      </Grid.RowDefinitions>
      <ListView x:Name="DeviceList" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" HasUnevenRows="True" RowHeight="150">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <controls:DeviceView></controls:DeviceView>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
      <Button x:Name="Refresh" Grid.Row="1" Text="Refresh devices" WidthRequest="200" HeightRequest="50"></Button>
    </Grid>
  </ContentPage.Content>
</ContentPage>